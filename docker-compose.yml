services:
  logforce-server:
    build:
      context: .
      dockerfile: Dockerfile.lumber
    container_name: logforce-perimeter
    ports:
      - "8081:8081"
    volumes:
      - ./output:/app/output
    environment:
      - LUMBER_MODEL_DIR=/app/models
      - STATIC_DIR=/app/ui
      - PORT=8081
      - PG_DSN=postgres://logforce:logforce@postgres:5432/logforce?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8081/api/health"]
      interval: 10s
      retries: 5

  postgres:
    image: postgres:16-alpine
    container_name: logforce-postgres
    environment:
      - POSTGRES_USER=logforce
      - POSTGRES_PASSWORD=logforce
      - POSTGRES_DB=logforce
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U logforce"]
      interval: 5s
      retries: 10

volumes:
  pgdata:
