# copy to .env and adjust for your host
PERIMETER_LOG_PATH=./ingestion/sample.log
PERIMETER_SINK_PATH=./output/normalized/perimeter-%Y-%m-%d.ndjson
VECTOR_DATA_DIR=./vector_data
LUMBER_MODEL_DIR=./models
PORT=8081
STATIC_DIR=./ui
# optional Wazuh
WAZUH_INDEXER_ENDPOINT=https://wazuh-indexer:9200
WAZUH_INDEXER_PASSWORD=changeme
CA_BUNDLE=./models/ca.crt
# optional cloud Postgres (Neon), enables PG backed stats plus query plus review
# PG_DSN=postgresql://user:pass@host/dbname?sslmode=require
